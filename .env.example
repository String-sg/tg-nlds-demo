# Supabase Configuration
# Get these from: Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (Admin operations - KEEP SECRET)
# Only use in server-side code, never expose to client
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database Connection (for migrations)
# Get from: Supabase Dashboard → Project Settings → Database → Connection String
DATABASE_URL=postgresql://postgres:your-password@db.your-project-id.supabase.co:5432/postgres
POSTGRES_URL_NON_POOLING=postgresql://postgres:your-password@db.your-project-id.supabase.co:5432/postgres

# OpenAI Configuration
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_ORG_ID=org-your-org-id-optional

# Development/Testing Configuration (Optional)
# Set to 'true' to use mock data instead of real database
NEXT_PUBLIC_PTM_MOCK_MODE=false
# UUID of the teacher to use in mock mode
NEXT_PUBLIC_PTM_MOCK_TEACHER_ID=

# Node Environment
NODE_ENV=development
